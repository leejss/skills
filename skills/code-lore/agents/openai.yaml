interface:
  display_name: "Code Lore"
  short_description: "Extract project-agnostic knowledge from code"
  default_prompt: "Use $code-lore to turn this code into a project-agnostic, source-backed technical article."
policy:
  allow_implicit_invocation: false
